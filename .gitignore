# artefactos de compilacion
build/

# cacheas y artefactos regenerables del taller
tools/downloads/
tools/__pycache__/
__pycache__/
tools/sheet.png
tools/import_sheet.png
tools/emitted_sprites.js
tools/demo_anim.js
tools/demo_compare.js

.DS_Store
tools/pmd_cache/
tools/pokeapi_cache/

# The sprite bundles ARE committed, and that is deliberate. They have to be
# served same-origin from GitHub Pages: release assets send no CORS headers at
# all, so a browser can never fetch() one, however convenient it would be to
# keep them out of the repo. Tested with an Origin header; there is no header
# to be had. Pages sends access-control-allow-origin: *.

# The loose per-species sprite packs are build INTERMEDIATES, not artefacts.
# They are what pack_bundle.py reads to build web/sprites-*.pak, and the .pak
# files are what actually gets distributed -- so tracking both puts the same
# ~105 MB of sprite data in the repo twice. Restore them with:
#
#   python3 tools/unpack_bundle.py       # from the committed .pak, no network
#
# or re-fetch from source, which is only needed when adding a region:
#
#   python3 tools/pack_pmd.py            # or: kanto / johto / hoenn / ...
#   python3 tools/make_thumbs.py
#
# YOU NEED THEM TO TAKE SCREENSHOTS. Without this directory the emulator draws
# every creature as a bare dex number, so tools/make_screens.sh would overwrite
# docs/screens/ with art-less images -- see CLAUDE.md § Screenshots.
#
# CREDITS.md recommends shipping the code and the scripts and letting each user
# package the sprites from the original sources, which is why the loose files
# stay out of the repo while the .pak bundles (which Pages must serve
# same-origin) stay in.
tools/sdcard/mons/*.bin

# A ready-to-copy mirror of the card's own root layout (mons/*.bin), generated
# from the same committed .pak bundles above for dragging onto a physical SD
# card with a reader instead of sending over USB. Same ~230 MB, same reason to
# keep it out of the repo.
/SDCARD/

# Emulator save files and downloaded generator intermediates: local scratch, not
# artefacts. They were committed by accident once.
tamapoke.nvs
tamapoke.nvs.*
tools/_*.svg
backups/

# A one-off compiled firmware image for flashing by hand, not a build target
# any script here produces or expects -- see whichever session note asked
# for it. Same reasoning as backups/: local, and not something to commit.
/build_output/

# tools/debugger/debugger_gui.py's own session logs and trend CSVs -- one
# pair per connect, local scratch like tamapoke.nvs and backups/ above.
tools/debugger/serial_logs/

# One-off bug-report bundles from the "Create bug report" button -- meant to
# be shared ad hoc (e.g. pasted to Claude), not committed.
/bugreports/

# Personal backup archives of the SD card contents -- the same sprite data
# tools/sdcard/mons/*.bin and /SDCARD/ above already deliberately exclude,
# just re-zipped. Redundant with the committed .pak bundles.
/SDCARD.7z
/SDCARD.zip
/tools/sdcard.zip

# WIP port to the Waveshare ESP32-S3-Touch-AMOLED-1.43-B (smaller panel,
# FT3168 touch) -- not wired into TamaPoke.ino yet, held back until it is.
/pin_config_amoled143b.h
/touch_ft3168.h
